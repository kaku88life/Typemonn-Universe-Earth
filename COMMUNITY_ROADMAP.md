# Type-Moon Holograph - Community Collaboration System
## 社群協作與 GameFi 獎勵系統規劃

**願景**: 一個結合 Wiki、DAO、Gamefi 的去中心化型月宇宙知識庫

---

## 📊 系統架構概覽

```
┌─────────────────────────────────────────────────────┐
│         Type-Moon Holograph (Interactive Globe)     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  1️⃣ 公開瀏覽層 (PUBLIC)                           │
│     └─ 所有人可查看位置、角色、編輯建議           │
│                                                     │
│  2️⃣ 社群協作層 (COMMUNITY)                         │
│     ├─ 編輯提案系統（新增、修改位置/角色）        │
│     ├─ 投票機制（社群審核內容）                   │
│     └─ 版本控制（追蹤編輯歷史）                   │
│                                                     │
│  3️⃣ 獎勵機制層 (GAMIFI)                            │
│     ├─ 貢獻度計分（Contribution Points）          │
│     ├─ 聲譽等級（Reputation Levels）              │
│     ├─ 虛擬代幣（TM-Token 或自訂名稱）            │
│     └─ 成就與徽章（Achievements）                 │
│                                                     │
│  4️⃣ 治理層 (GOVERNANCE)                            │
│     ├─ 多籤審核（需要多人同意）                   │
│     ├─ 提案投票（社群民主化決策）                 │
│     └─ 爭議解決（編輯衝突時的仲裁）              │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 核心機制詳解

### 1️⃣ **編輯提案系統** (Edit Proposal System)

#### 使用者流程：
```
使用者 → 發起編輯提案 → 提案進入「待審」狀態
                    ↓
         社群投票（3-7 天期限）
         ├─ 需要 50%+ 同意票
         ├─ 需要至少 3 個投票者
         └─ 需要足夠的信譽值 (Reputation ≥ 50)
                    ↓
         投票通過 → 編輯生效
         投票駁回 → 提案歸檔，反饋給編輯者
```

#### 可編輯內容（三大類）：

**A. 位置 (Locations)**
```typescript
{
  id: string;
  name: string;           // ✏️ 可編輯
  description: string;    // ✏️ 可編輯
  coordinates: {          // ✏️ 可編輯
    lat: number;
    lng: number;
  };
  type: 'City' | 'Landmark' | 'MagicAssociation' | 'Singularity';  // ✏️
  year_start: number;     // ✏️ 可編輯
  year_end: number;       // ✏️ 可編輯
  continent: string;      // ✏️ 可編輯
  country: string;        // ✏️ 可編輯
  world_lines: string[];  // ✏️ 可編輯

  // 社群欄位
  editHistory: Edit[];    // 所有編輯記錄
  createdBy: string;      // 原始貢獻者 ID
  lastEditBy: string;     // 最後編輯人
  lastEditAt: Date;
  status: 'approved' | 'pending' | 'rejected';
}
```

**B. 角色 (Characters)**
```typescript
{
  id: string;
  name: string;
  description: string;
  image_url: string;
  class: 'Saber' | 'Archer' | 'Lancer' | ...; // Servant Class
  rarity: 1 | 2 | 3 | 4 | 5;
  relatedLocations: string[]; // 位置 ID
  relatedWorks: string[];     // 作品 ID

  editHistory: Edit[];
  status: 'approved' | 'pending' | 'rejected';
}
```

**C. 工作日誌 (Work Entries / Lore Notes)**
```typescript
{
  id: string;
  title: string;
  content: string;
  relatedLocation?: string;
  relatedCharacter?: string;
  worldLine: string;
  year: number;

  editHistory: Edit[];
  status: 'approved' | 'pending' | 'rejected';
}
```

---

### 2️⃣ **投票與審核機制** (Voting & Moderation)

#### 投票規則：

```typescript
interface ProposalVote {
  id: string;
  proposalId: string;
  content: string;        // 提案內容（diff 格式）

  votes: {
    approve: Set<string>; // 贊成的用戶 ID
    reject: Set<string>;  // 反對的用戶 ID
  };

  createdAt: Date;
  endsAt: Date;          // 投票截止日期（3-7 天）

  votingPower: {
    // 投票權重 = 用戶的信譽值
    [userId]: number;    // userId -> reputation score
  };

  // 自動結算規則
  requirements: {
    minVoters: 3;                    // 至少 3 人投票
    approvalThreshold: 0.5;          // 需要 50% 同意
    minReputation: 50;               // 投票者信譽要求
  };
}
```

#### 特殊投票場景：

**場景 1: 編輯衝突**
```
情況：A 和 B 同時提出對同一位置的相反編輯
→ 由社群進行「決鬥投票」
→ 贏者的編輯被採納
→ 輸者得到反饋，可以修改後重新提交
```

**場景 2: 內容爭議**
```
情況：社群認為某個編輯內容不準確或有爭議
→ 任何信譽 ≥ 50 的用戶可以發起「異議投票」
→ 如果異議票超過 60%，編輯被回退
→ 編輯者需要提供更多證據或改進提案
```

---

### 3️⃣ **獎勵系統** (Reward Mechanism)

#### 貢獻度計分 (Contribution Points)

```typescript
interface UserProfile {
  id: string;
  username: string;
  email: string;

  // 成就指標
  stats: {
    totalEdits: number;           // 提出的編輯數
    approvedEdits: number;        // 通過的編輯數
    approvalRate: number;         // 通過率百分比
    totalVotes: number;           // 參與投票數
    helpfulVotes: number;         // 獲得「有幫助」票數
  };

  // 信譽系統
  reputation: number;             // 0-1000 分
  level: 'Apprentice' | 'Editor' | 'Expert' | 'Archivist' | 'Keeper';

  // 虛擬貨幣（GameFi）
  tokens: {
    balance: number;              // 當前持有量
    earned: number;               // 累計賺取量
    spent: number;                // 累計花費量
  };

  // 徽章與成就
  badges: Badge[];
  achievements: Achievement[];

  // 貢獻等級
  tier: 1 | 2 | 3 | 4 | 5;      // 1=新手, 5=傳奇
}
```

#### 代幣獲取方式：

| 行為 | 獲得代幣 | 信譽值 | 說明 |
|------|--------|--------|------|
| 首次編輯通過 | 10 TM | +20 | 新用戶獎勵 |
| 一般編輯通過 | 5 TM | +10 | 常規貢獻 |
| 大型編輯通過 (多項修改) | 25 TM | +50 | 增加 5+ 條信息 |
| 編輯被投票者「有幫助」 | 3 TM | +5 | 社群認可 |
| 參與投票 | 0.5 TM | +2 | 治理參與 |
| 投票決策正確 | 2 TM | +5 | 投票符合最終結果 |
| 編輯被引用 (被其他人基於你的編輯繼續編輯) | 5 TM | +15 | 知識傳承 |

#### 信譽等級 (Reputation Tiers)：

```
🌱 Apprentice (0-100)
   ├─ 新加入的編輯者
   ├─ 可提案編輯，但需要多數同意
   └─ 無法投票（信譽不足）

📚 Editor (101-300)
   ├─ 貢獻 5+ 個通過的編輯
   ├─ 可投票，且投票權重 = 信譽值
   └─ 解鎖高級編輯權限

🎓 Expert (301-600)
   ├─ 貢獻 20+ 個通過的編輯，approval rate ≥ 80%
   ├─ 投票權重翻倍
   └─ 可作為「審核委員」協助決策

👑 Archivist (601-850)
   ├─ 長期活躍貢獻者 (≥ 3 個月)
   ├─ 投票權重 3 倍
   └─ 可發起「內容爭議投票」

👸 Keeper (851-1000)
   ├─ 傳奇貢獻者，貢獻 100+ 編輯
   ├─ 投票權重 5 倍，投票結果自動加權
   └─ 可提議系統改進、新增位置分類
```

#### 代幣用途 (TM-Token Utility)：

```
1. 優先權 (Priority)
   └─ 花 10 TM 加速投票期限（從 7 天縮短為 3 天）

2. 特殊貼標 (Special Badge)
   └─ 花 50 TM 為編輯添加「官方推薦」標籤

3. 查看私密筆記 (Premium Content)
   └─ 花 5 TM 查看某位編輯者的「編輯筆記」（為什麼這樣改）

4. 內容鎖定 (Lock Proposal)
   └─ 花 20 TM 提議將某個位置「鎖定」，防止頻繁編輯

5. 交換獎勵 (Reward Shop)
   └─ 花 100 TM 兌換「社群認可證書」或「排行榜排位」

（代幣不涉及金錢交易，純粹遊戲化獎勵）
```

---

### 4️⃣ **成就系統** (Achievements & Badges)

```typescript
interface Badge {
  id: string;
  name: string;
  description: string;
  icon: string;
  unlockedAt?: Date;
  rarity: 'common' | 'rare' | 'epic' | 'legendary';
}

interface Achievement {
  id: string;
  title: string;
  description: string;
  condition: string;      // 條件描述
  unlockedAt?: Date;
  progress?: number;      // 0-100 進度百分比
}
```

#### 徽章列表範例：

```
🌟 新手編輯 (Newcomer)
   └─ 完成第一個編輯提案

🎯 精準編輯 (Accurate Editor)
   └─ 獲得 5 個「有幫助」票

📍 地理大師 (Geography Master)
   └─ 編輯超過 10 個位置

👥 角色百科 (Character Collector)
   └─ 編輯超過 20 個角色

🏆 治理參與者 (Governance Participant)
   └─ 參與 50 次投票

⚡ 快速編輯 (Speed Runner)
   └─ 編輯在 24 小時內通過

🎨 完整編輯 (Complete Editor)
   └─ 添加完整的描述、圖片、引用

🌍 環球探險家 (Global Explorer)
   └─ 編輯跨越 5+ 個大陸的位置

💎 傳奇貢獻 (Legendary Contributor)
   └─ 達到 Archivist 或更高等級
```

---

## 📱 前端 UI 組件規劃

### 需要新增的組件：

```
src/components/
├── Community/
│   ├── ProposalForm.tsx          # 提交編輯提案
│   ├── ProposalList.tsx          # 待審批提案列表
│   ├── VotingCard.tsx            # 投票卡片
│   ├── DiffViewer.tsx            # 展示編輯前後對比
│   └── ProposalDetail.tsx        # 提案詳情頁
│
├── GameFi/
│   ├── UserProfile.tsx           # 用戶個人資料卡
│   ├── ReputationBadge.tsx       # 信譽等級徽章
│   ├── TokenBalance.tsx          # 代幣餘額展示
│   ├── AchievementShowcase.tsx   # 成就展示牆
│   ├── LeaderBoard.tsx           # 排行榜
│   └── RewardShop.tsx            # 獎勵商城
│
└── UI/
    └── TimeAxisAdvanced.tsx      # ⭐ 高級時間軸（支持 4 大階段）
```

---

## ⏰ **高級時間軸設計** (Time Axis V2)

### 設計方案：動態單位時間軸

你的想法非常聰慧！根據「距離當前時間」動態調整單位：

```
當前時間（2026）
    ↓
[近代] 100 年 → 10 年 → 1 年 → 6 個月 → 1 個月
       2026   2126    2226   2276     2326

       ↕️ 用戶可通過按鈕切換階段

[中世] 1000 年 → 100 年 → 50 年
       500BC   1500AD  2000AD

[上古] 100 萬年 → 1 萬年 → 1000 年
       1000000BC  10000BC  1000BC

[太古] 無限遙遠（超越時間軸的位置）
```

### 實現方式：四層階段系統

```typescript
interface TimelineStage {
  id: string;
  name: string;              // "近代", "中世", "上古", "太古"
  description: string;

  // 時間範圍
  startYear: number;
  endYear: number;

  // 細分單位
  unitSize: 'month' | 'quarter' | 'year' | '10years' | '100years' | '1000years' | 'million';

  // 視覺風格
  color: string;
  backgroundColor: string;
  icon: string;
}

interface Location {
  // ... 原有欄位
  stage: 'modern' | 'medieval' | 'ancient' | 'mythical'; // 自動計算
}
```

### 階段定義：

| 階段 | 時間範圍 | 主要單位 | 特點 |
|------|---------|--------|------|
| **近代** (Modern) | 1500-2300 | 年/月 | 詳細記錄，大量事件 |
| **中世** (Medieval) | -2000-1500 | 50年/100年 | 中等粒度，歷史較完整 |
| **上古** (Ancient) | -200000-2000 | 1000年/萬年 | 粗粒度，考古推測 |
| **太古** (Mythical) | ∞ 之前 | 無限制 | 神話時代，超越時間 |

### 交互邏輯：

```
使用者點擊「切換階段」按鈕
    ↓
時間軸平滑過渡到新階段
    ↓
地球上的位置根據年份重新篩選和排列
    ↓
新增位置顯示/隱藏（根據是否在該階段時間範圍內）
    ↓
可繼續向上/向下鑽探細節（年份精度逐漸增加）
```

---

## 🛠️ 技術棧與實現路線

### 後端需求：

```typescript
// 新增 Prisma 模型
model Proposal {
  id: String @id @default(cuid())
  type: 'location' | 'character' | 'note'
  contentId: String           // 要編輯的內容 ID

  // 編輯內容（JSON diff 格式）
  changes: Json
  changelog: Json

  // 狀態
  status: 'pending' | 'approved' | 'rejected'
  proposedBy: String          // 用戶 ID
  approvedBy: String[]        // 批准的投票者
  rejectedBy: String[]        // 反對的投票者

  createdAt: DateTime
  endsAt: DateTime             // 投票截止時間

  // 關聯
  location?: Location @relation(...)
  character?: Character @relation(...)
  votes: Vote[]

  @@index([status, endsAt])
}

model Vote {
  id: String @id @default(cuid())
  proposalId: String
  userId: String

  decision: 'approve' | 'reject'
  weight: Int                 // 投票權重 = 用戶信譽值
  comment: String?

  createdAt: DateTime
  proposal: Proposal @relation(fields: [proposalId], references: [id], onDelete: Cascade)

  @@unique([proposalId, userId])  // 每個提案每個用戶只能投一票
}

model UserProfile {
  id: String @id @default(cuid())
  username: String @unique
  email: String @unique

  // 信譽與獎勵
  reputation: Int @default(0)
  tokenBalance: Int @default(0)

  // 統計
  totalEdits: Int @default(0)
  approvedEdits: Int @default(0)

  // 關聯
  proposals: Proposal[]
  votes: Vote[]
  badges: Badge[]
  achievements: Achievement[]

  createdAt: DateTime
  updatedAt: DateTime
}

model Badge {
  id: String @id @default(cuid())
  userId: String
  badgeType: String
  unlockedAt: DateTime
  user: UserProfile @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

### API Endpoints：

```
POST   /api/proposals                 # 提交新提案
GET    /api/proposals                 # 列出所有提案
GET    /api/proposals/:id             # 獲取提案詳情
POST   /api/proposals/:id/vote        # 投票
GET    /api/proposals/:id/votes       # 查看投票情況
PATCH  /api/proposals/:id/approve     # 手動批准（管理員）

GET    /api/users/me                  # 當前用戶信息
GET    /api/users/:id                 # 用戶公開資料
PATCH  /api/users/me                  # 更新個人資料
GET    /api/users/leaderboard         # 排行榜

GET    /api/achievements              # 成就列表
POST   /api/achievements/claim        # 領取成就
GET    /api/shop/items                # 商城物品列表
POST   /api/shop/purchase             # 購買物品
```

---

## 📈 開發優先級

### Phase 1️⃣: MVP (1-2 週)
- ✅ Proposal 提案系統基礎
- ✅ 簡單投票機制
- ✅ 用戶信譽積分計算

### Phase 2️⃣: 社群功能 (2-3 週)
- ✅ 高級時間軸系統
- ✅ 排行榜和用戶資料卡
- ✅ 編輯歷史追蹤

### Phase 3️⃣: GameFi (3-4 週)
- ✅ 代幣獎勵系統
- ✅ 成就與徽章系統
- ✅ 商城與代幣兌換

### Phase 4️⃣: 治理與爭議解決 (4-6 週)
- ✅ 多籤審核機制
- ✅ 爭議投票系統
- ✅ 社群提案（元治理）

---

## 🎯 最終願景

這個系統會變成：

```
🌍 一個開源的、社群驅動的、激勵完善的型月知識库

特點：
1️⃣ 去中心化治理 - 社群投票決定內容
2️⃣ 完全透明 - 所有編輯歷史可追溯
3️⃣ 遊戲化激勵 - 貢獻者獲得代幣、成就、排位
4️⃣ 小眾同好社區 - 愛好者互動、交流、協作
5️⃣ 可持續發展 - 通過代幣激勵保持活躍度

最後可能演變為一個「Fate 社群 + 知識庫 + 遊戲」的混合體！
```

---

## 📝 後續討論點

1. **代幣名稱**：是否用 `TM-Token`（Type-Moon），還是更有趣的名字如 `GrailPoint` 或 `ArcusToken`？

2. **初始代幣分配**：新用戶是否初始給予 10-20 個代幣作為「啟動資金」？

3. **代幣總供應量**：是否設置上限（如 100 萬 TM），還是無限供應？

4. **跨平台**：未來是否考慮在 Discord/Twitter 上同步成就？

5. **安全性**：是否需要「內容驗證」機制（例如，編輯內容必須附帶來源鏈接）？

還有其他想法或調整嗎？
